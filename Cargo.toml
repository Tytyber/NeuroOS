[package]
name = "neuros"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = { version = "0.11", features = ["uefi_x86_64"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"